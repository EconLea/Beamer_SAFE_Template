% A fork of https://github.com/anishathalye/gemini

\documentclass[final]{beamer}

% ====================
% Packages
% ====================

\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[size=custom,width=120,height=72,scale=1.0]{beamerposter}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.14}
\usepackage{anyfontsize}
\usepackage{qrcode}               % QR code generation

%─────────────────────────────────────────────────────────────────────────────
% Citations & Bibliography
%─────────────────────────────────────────────────────────────────────────────
\usepackage{csquotes}  % Recommended with biblatex for proper quoting
\usepackage[backend=biber,style=apa,citestyle=apa,uniquename=false,backref=false]{biblatex}
\addbibresource{C:/Users/LeaPaoli/Repos/BibFiles/All_bib.bib}

%─────────────────────────────────────────────────────────────────────────────
% Load Sty Files and Themes
%─────────────────────────────────────────────────────────────────────────────
\usefonttheme{serif}               % Use a serif font theme for Beamer
\usepackage{styles/beamerthemegemini_SAFE}
\usepackage{styles/beamercolorthemegemini_SAFE}


%─────────────────────────────────────────────────────────────────────────────
% Color Definitions & Logo
%─────────────────────────────────────────────────────────────────────────────
\newif\ifwithlogo
\withlogotrue % Set condition to \withlogofalse to remove logos

\ifwithlogo
  % logos are add to the header after \being{document}. 

  % Set which defined color to use as "primary" and "secondary":
  \colorlet{primary}{white}         % Main color, change 'purple' to 'red', 'green', etc.
  \colorlet{secondary}{pastelsafegreen}  % Color of hightlighted blocks
  \colorlet{tertiary}{safegreen}
  \colorlet{titlefont}{safeblue}
  \colorlet{footline}{safeblue}
\else
  % Set which defined color to use as "primary" and "secondary":
  \colorlet{primary}{safeblue}         % Main color, change 'purple' to 'red', 'green', etc.
  \colorlet{secondary}{pastelsafegreen}  % Color of hightlighted blocks
  \colorlet{tertiary}{safegreen}
  \colorlet{titlefont}{white}
  \colorlet{footline}{white}
\fi


%─────────────────────────────────────────────────────────────────────────────
% Set length of columns 
%─────────────────────────────────────────────────────────────────────────────
% Adjust the number of columns here
\newcommand{\numcolumns}{3} 

% Calculate widths dynamically
\newlength{\sepwidth}
\newlength{\colwidth}

% (N+1)*sep + N*col = \paperwidth
% Let sepwidth = s => colwidth = c = (\paperwidth - (N+1)*s)/N
% Set sepwidth as fraction of paperwidth (e.g., 0.02\paperwidth)
\setlength{\sepwidth}{0.02\paperwidth}

% Compute colwidth = (\paperwidth - (\numcolumns + 1)*\sepwidth)/\numcolumns
\pgfmathsetmacro{\calccolwidth}{(\paperwidth - (\numcolumns + 1)*\sepwidth) / \numcolumns}
\setlength{\colwidth}{\calccolwidth pt}
\newcommand{\separatorcolumn}{\begin{column}{\sepwidth}\end{column}}


%─────────────────────────────────────────────────────────────────────────────
% Set length of columns 
%─────────────────────────────────────────────────────────────────────────────
\title{Some fancy title: followed by some more text}

\author{Alyssa P. Hacker \inst{1} \and Ben Bitdiddle \inst{2} \and Lem E. Tweakit \inst{2}}

\institute[shortinst]{\inst{1} Leibniz Institute for Financial Research SAFE \samelineand \inst{2} Goethe University Frankfurt}


%─────────────────────────────────────────────────────────────────────────────
% Footer
%─────────────────────────────────────────────────────────────────────────────
\footercontent{
  \href{https://www.example.com}{https://www.example.com} \hfill
  ABC Conference 2025, New York --- XYZ-1234 \hfill
  \href{mailto:alyssa.p.hacker@example.com}{alyssa.p.hacker@example.com}}


%─────────────────────────────────────────────────────────────────────────────
% Body
%─────────────────────────────────────────────────────────────────────────────
\begin{document}

\ifwithlogo
% Don't use \logoright and \logoleft here, as they don't offer sufficient control 
\addtobeamertemplate{headline}{}
{
    \begin{tikzpicture}[remember picture, overlay]
      % Top left: Goethe logo, 2cm from edges
      \node[anchor=north west, xshift=3cm, yshift=-\headeroffset + 0.3cm] at (current page.north west) {
        \includegraphics[height=5cm]{figures/goethe_logo.png}
      };

      % Top right: SAFE logo, 2cm from edges
      \node[anchor=north east, xshift=-3cm, yshift=-\headeroffset + 0.3cm] at (current page.north east) {
        \includegraphics[height=5cm]{figures/safe_logo.jpg}
      };
    \end{tikzpicture}
}
\fi

\begin{frame}[t]
\begin{columns}[t]
\separatorcolumn

\begin{column}{\colwidth}
\input{content/Column1.tex}
\end{column}

\separatorcolumn

\begin{column}{\colwidth}
\input{content/Column2.tex}
\end{column}

\separatorcolumn

\begin{column}{\colwidth}
\input{content/Column3.tex}
\end{column}

\separatorcolumn
\end{columns}
\end{frame}

\end{document}
